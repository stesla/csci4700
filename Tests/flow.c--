main() {
  x = 1;
  y = 0;

  if (x)
    write("ok");
  else
    write("not ok");

  if (y)
    write("not ok");
  else
    write("ok");

  if (y)
    write("not ok");
  else if (x)
    write("ok");
  else
    write("not ok");

  write("i from 0 to 2");
  i = 0;
  while(i <= 2)
    {
      write(i);
      i++;
    }

  write("j from 2 to 0");
  for (j = 2; j >= 0; --j)
    {
      write(j);
    }

  for (;;)
    ;
}
